# Enum & Match

- Enum
    - Define & init
    - Enum with data
    - Multi-type & method in enum
    - Option
- Match
    - Bind value in match
    - Option in match
    - Wildcard operator: `_`
    - If-let
